# ResNet18 behavior-cloning control, not a paper reproduction.
defaults:
  - base
  - override /vision_encoder@_global_.vision_encoder: cnn_resnet
  - override /action_decoder@_global_.action_decoder: waypoint
  - override /temporal_encoder@_global_.temporal_encoder: identity
  - _self_
